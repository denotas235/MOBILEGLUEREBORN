<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nexus.vulkan"
    android:versionCode="1"
    android:versionName="1.0.0-NVR">

    <!-- Minimum SDK: Android 5.0 (Lollipop) - Required for Vulkan and modern OpenGL ES -->
    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="31"
        android:maxSdkVersion="34" />

    <!-- Permissions for storage access (shader cache, configs) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Request legacy external storage for Android 10+ -->
    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:hasFragileUserData="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SteampunkTheme"
        android:usesCleartextTraffic="true">

        <!-- Main Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|locale"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/SteampunkTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service for loading native library -->
        <service
            android:name=".NexusVkService"
            android:enabled="true"
            android:exported="false" />

        <!-- Receiver for boot complete (optional: load settings on boot) -->
        <receiver
            android:name=".NexusVkBootReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Meta-data for Vulkan -->
        <meta-data
            android:name="android.app.vulkan"
            android:value="true" />

    </application>

</manifest>
